I am designing a highly rigorous, statistical simulation comparison of two deep ResNet-based adaptive controllers for quadcopter target tracking at the acceleration-guidance level. The target is a desired trajectory generated by a bounded autonomous subsystem. System identification of the ResNet weights is strictly NOT a goal; only local tracking error minimization is prioritized.
Your goal is to help me build the ROS 2 node, tuning pipeline, and execution orchestration. I am using a Gazebo PX4 SITL stack that I did not design. You will need to figure out or grill me on how to interface with it. Ask for my verbatim JAX projection code when you need it.
CONTROLLERS & KINEMATICS
$q \in \mathbb{R}^3$ is 3D position, $\dot{q} \in \mathbb{R}^3$ is 3D linear velocity. Both measured via PX4 EKF2 (noisy). No $\ddot{q}$ measurement.
$q_d \in \mathbb{R}^3$ is the desired 3D position, $\dot{q}_d \in \mathbb{R}^3$ is desired 3D linear velocity.
Baseline controller is:

$$u(t) = \Phi(x(t), \hat{\theta}(t)) + K_P e(t) + K_D \dot{e}(t) + \int_{t_0}^t \left( K_I e(\tau) + K_{\text{RISE}} \text{sgn}(r_1(\tau)) \right) d\tau$$ 
Developed controller is:

$$u(t) = K_P e(t) + K_D \dot{e}(t) + \int_{t_0}^t \left( K_I e(\tau) + K_{\text{RISE}} \text{sgn}(r_1(\tau)) + \Phi(\kappa(\tau), \hat{\theta}(\tau)) \right) d\tau$$ 
where:

$$K_P \triangleq k_1 k_2 + k_1 k_3 + k_2 k_3 + 1$$
$$K_I \triangleq k_1 k_2 k_3 + k_1$$
$$K_D \triangleq k_1 + k_2 + k_3$$
$$\kappa \triangleq \left[ q^\top, \dot{q}^\top, q_d^\top, \dot{q}_d^\top, u^\top \right]^\top \in \mathbb{R}^{15}$$    
$$x \triangleq \left[ q^\top, \dot{q}^\top, q_d^\top, \dot{q}_d^\top \right]^\top \in \mathbb{R}^{12}$$   
$$e \triangleq q_d - q$$
$$r_1 \triangleq \dot{e} + k_1 e$$
Update law (used for both):

$$\dot{\hat{\theta}} = \text{proj}_{\mho}\left(\Gamma \left. \frac{\partial \Phi}{\partial \theta} \right|^\top r_1 - \sigma_{\text{mod}} \hat{\theta}\right)$$
$$\mho \triangleq \bar{B}_{\bar{\theta}}(0_p)$$
ENVIRONMENT & DYNAMICS
•	Operating Space: 25 m (Y) by 12 m (X) by 8 m (Z). Origin at floor center. Z is positive down (NED frame), so $Z \in [-7, 0]$ .
•	Safety Boundary: 1 m buffer from all walls.
•	Trajectory: 3D Lissajous curve. Period $T = 20$ s. $t_f = 60$ s.
•	$\ddot{x}_d = -\omega_x^2 x_d$ (Amplitude 3 m, $\omega_x \approx 0.628$ rad/s)
•	$\ddot{y}_d = -\omega_y^2 y_d$ (Amplitude 8 m, $\omega_y \approx 0.314$ rad/s)
•	$\ddot{z}_d = -\omega_z^2 (z_d - z_c)$ (Amplitude 0.5 m, $z_c = -4$ m, $\omega_z \approx 0.314$ rad/s)
•	Cost Function: $J = \int_{0}^{t_{\text{fail}}} \left( t \cdot q_e \|e(\tau)\|^2 + r_u \|u(\tau)\|^2 \right) d\tau + W_{\text{fail}} (t_f - t_{\text{fail}})^2$
CUSTOM RESNET LIBRARY (jax_resnet)
I have a strictly non-editable custom library. You must import and use these exact functions wherever possible:
•	resnet_network(theta, x, d_in, hidden_width, d_out, b, k_0, k_i, h_act_func, o_act_func, shortcut_act_func): The forward pass.
•	get_total_parameters(d_in, hidden_width, d_out, b, k_0, k_i): Returns integer parameter count.
•	init_resnet_weights(key, d_in, hidden_width, d_out, b, k_0, k_i, h_method, o_method): Returns the flat initialized theta vector. You MUST use this to generate weights.
STRICT EXPERIMENT CONSTRAINTS
	1.	Exact Mathematics: Do NOT approximate the signum function. Do NOT filter control $u(t)$. Calculate $\Delta t$ dynamically using ROS 2 timestamps to handle OS jitter.
	2.	Integration: Use the Trapezoidal rule for both integrals.
	3.	State Machine: Wait for PX4 to spawn/EKF to settle $\rightarrow$ Takeoff to hover $\rightarrow$ Wait 1 second to ensure safe box entry $\rightarrow$ Lock desired heading (yaw) to current heading via P-controller $\rightarrow$ Enable offboard control and execute experiment.
	4.	Clamping/Anti-Windup: If any component of acceleration $u(t)$ is saturated, the integral term must stop accumulating (hold current value) AND the neural network weights must stop updating ($\dot{\hat{\theta}} = 0$).
	5.	Statistical Fairness (Seeds): For Optuna tuning, use exactly ONE frozen random seed to generate ONE fixed state initial condition and ONE fixed NN weight vector per controller. For final statistical validation, generate 30 frozen paired scenarios. $u_0 = K_P e(t_0) + K_D \dot{e}(t_0)$.
	6.	Tuning: Optimize linear gains without NN first. Freeze them, then optimize NN parameters using the ITAE cost.
	7.	JAX Management: Use jax.config.update("jax_compilation_cache_dir", ...) to prevent JIT tracing freezes on restarts. No AOT boilerplate.
CRITICAL UNKNOWNS YOU MUST HELP ME SOLVE
•	Initial Conditions: I do not know how to inject Gazebo with specific random initial conditions for paired trials without breaking PX4's EKF2. We must figure this out before coding.
•	Saturation Limits: I do not know where to ask PX4 what its maximum acceleration or speed limits are for the clamping algorithm.
CODE PRACTICES
Minimalism. No silent errors—crash or scream on failure. Very few comments. Strict type hinting. NO default arguments ever. DO NOT save neural network weights to disk (log final cost to SQLite). The Optuna orchestrator must killall -9 everything between trials.

Ask if any characters above did not render. Here are the instructions that came with the PX4-Gazebo SITL.

Instructions for building docker image and running sim environment:

Note - these instructions require a ubuntu 22+ environment with docker installed. 

***********************************
Building the sim environment
***********************************

1. Copy "voxl-px4-sitl" folder to home directory (required for setup scripts to work)

2. If you don't have a ros2 docker image to start with, install it with the following command:

    sudo docker pull osrf/ros:humble-desktop

If using an existing ros2 docker image as your base image, update "run-ros2-container.sh" script as follows:

    sudo docker run --rm -it --net=host --ipc=host --pid=host --privileged -v /dev/shm:/dev/shm -e DISPLAY=$DISPLAY -v /dev/input:/dev/input:rw -v /tmp/.X11-unix:/tmp/.X11-unix:ro -v $SIM_DIRECTORY:/home/root:rw -w /home/root --name=ros2humble <the name of your ros2 docker image> /bin/bash -l

3. Open a terminal, navigate to the voxl-px4-sitl directory, make shell scripts executable and open a container with the ros2 base image

    cd ~/voxl-px4-sitl
    chmod +x background-scripts/*
    chmod +x run*
    chmod +x spawn*
    chmod +x shutdown*
    chmod +x start*
    chmod +x update*
    chmod +x install*
    chmod +x connect*    
    ./run-ros2-container.sh

4. From within the docker container, clone modalai's fork of PX4 (same firmware running on Sentinel vehicles) and update the submodules

    git clone https://gitlab.com/voxl-public/voxl-sdk/services/voxl-px4.git -b sdk-1.4.0
    cd voxl-px4
    git submodule update --init --recursive

5. Install PX4 dependencies (without installing simulation tools)

    cd px4-firmware
    ./Tools/setup/ubuntu.sh --no-sim-tools

6. Return to the mounted home directory and run the helper script to install Gazebo Harmonic separately

    cd /home/root
    ./install_gz_harmonic.sh

7. Clone and build the appropriate DDS-Agent firmware (used to bridge PX4 UORB topics to ROS domain), this will take a few minutes

    git clone -b v2.4.3 https://github.com/eProsima/Micro-XRCE-DDS-Agent.git
    cd Micro-XRCE-DDS-Agent
    mkdir build
    cd build
    cmake ..
    make
    sudo make install
    sudo ldconfig /usr/local/lib/

8. Return to the mounted home directory and run the helper script to apply bug-fixes to px4 firmware

    cd /home/root
    ./update-px4-files.sh

9. Navigate to the px4 firmware and build the SITL binaries.  If the build process was performed successfully, this will open a Gazebo instance and spawn a model tied to a px4_sitl instance.  Exit w/ CTRL+C to close the simulation

    cd /home/root/voxl-px4/px4-firmware 
    make px4_sitl gz_sentinel_vision

10. Build the px4_msgs ROS package required to write ROS packages that can interface with px4_sitl instances

    source /opt/ros/humble/setup.bash
    cd /home/root/ros2_ws
    colcon build --packages-select px4_msgs --symlink-install

11. Install ros packages for interfacing with gazebo

    apt install ros-humble-ros-gzharmonic

12. Install dependencies and build ros minimal turtlebot simulation package 

    apt-get install ros-humble-joint-state-publisher
    cd /home/root/tb_sim_custom
    colcon build --symlink-install

        *****************
        (Optional) Before committing the new image, it can be helpful to add the following line to the end of the .bashrc file:

            source /usr/share/colcon_argcomplete/hook/colcon-argcomplete.bash

        This enables tab completion when building ros packages.  To edit files within the docker container that aren't mounted from your pc's filesystem, install vim (apt install vim) or another command line text editor and edit the file (~/.bashrc) from the terminal running the container
        *****************

13. The build is now complete.  Commit the running docker container to a new image.  Do not exit from the docker container.  Open a new terminal and commit the new docker image

    sudo docker commit ros2humble voxl-px4-sitl:sdk1.4-v1

14. To confirm the image was successfully committed, navigate to the voxl-px4-sitl directory and use the helper script to open a container with the new image

    cd ~/voxl-px4-sitl
    ./run-px4-sim-container.sh



If the new image was successfully committed, exit both containers.



***********************************
Using the sim environment
***********************************

1. Open a terminal, navigate to the sim environment directory and run the docker container

    cd ~/voxl-px4-sitl
    ./run-px4-sim-container.sh

2. Edit the file ~/voxl-px4-sitl/spawn-locations.env to set the number of each vehicle type you'd like to spawn within the sim environment and their spawn locations

3. Open another terminal, navigate to the sim environment directory and run the bringup script for the sim environment.  This will start a gazebo instance, spawn the number of models selected, run a px4_sitl instance for each spawned quad model, and start the DDS agent to bridge PX4 UORB topics to ROS

    cd ~/voxl-px4-sitl
    ./spawn-sim-env.sh

4. From the terminal that is running the docker container, source the ros-sources file and run a ros package to interact with the models in gazebo

    source ros-sources.sh
    ros2 launch your_package your_launch_file.launch.py

